import React from "react";
import ReactDOM from "react-dom/client";
import ClosingReport from "./ClosingReport.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ClosingReport />
  </React.StrictMode>
);
